import cv2
import numpy as np
from loguru import logger

# img = cv2.imread('./datasets/refuge_datasets/refuge_label/V0001.png', 0)
#
# img = 255 - img
# (rows, cols) = np.where(img == 127)
# img[rows, cols] = 1
# (rows, cols) = np.where(img == 255)
# img[rows, cols] = 2

# logger.add('./log/runtime_{time}.log', rotation='00:00')
# logger.info('info')

s_list = ['fdaf', 'afdsa', 'adfa']
print(','.join(s_list))
